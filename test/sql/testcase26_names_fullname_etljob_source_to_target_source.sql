spark.sql("SELECT src.ID as ID, src.BIRTHDATE as BIRTHDATE, src.PREFIX as PREFIX, src.PREFIX as PREFIX, cast(src.FIRST as None) as FIRST, src.LAST as LAST, src.SUFFIX as SUFFIX, src.GENDER as GENDER, src.STATE as STATE, NVL(PREFIX,'')||' '||NVL(FIRST,'')||' '||NVL(LAST,'')||' '||NVL(SUFFIX,'') as FULLNAME FROM OracleDemoTiger.sys.orc_patients_db src  ")
